import { Repository } from "typeorm"
import { AppDataSource } from "../data-source"
import { Client } from "../entities"


class ContactServices {
    registerContact = async (contactData: , clientId: number) => {
        
    }
}